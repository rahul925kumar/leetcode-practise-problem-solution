/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    let a = s.split(' ');
    let n = s.split(' ').length;
    let count = 0;
    for(let i =0; i<n; i++){
        if(a[i] !== ''){
            count = a[i].length;
        }
    }
    return count;
};
